# Информационная система электронного расписания занятий

## Описание проекта

Курсовая работа по дисциплине "Технология разработки программного обеспечения". Тема: Информационная система электронного расписания занятий.

Веб-приложение для управления расписанием занятий в образовательном учреждении (колледж, техникум, вуз).

## Функциональность

- Создание и редактирование расписания занятий
- Управление справочниками:
  - Преподаватели
  - Аудитории
  - Дисциплины
  - Учебные группы
- Предотвращение пересечений по аудиториям
- Предотвращение пересечений по преподавателям
- Просмотр расписания с фильтрацией по группам

## Технологии

- Python 3
- Flask (веб-фреймворк)
- PostgreSQL (база данных)
- SQLAlchemy (ORM)
- HTML/CSS (интерфейс)

## Требования

- Python 3.8+
- PostgreSQL 12+
- pip (менеджер пакетов Python)

## Установка

1. Клонируйте репозиторий:
```
bash
git clone <URL_репозитория>
cd kurs
```

2. Установите зависимости:
```
bash
pip install -r requirements.txt
```

3. Настройте подключение к базе данных в файле `.env`:
```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=schedule_db
DB_USER=postgres
DB_PASSWORD=123
```

4. Создайте базу данных в PostgreSQL:
```
sql
CREATE DATABASE schedule_db;
```

5. Создайте таблицы в базе данных:
```
bash
python create_db.py
```

## Запуск

Запустите приложение:
```
bash
python app.py
```

Откройте в браузере: http://127.0.0.1:5000

## Структура проекта

```
kurs/
├── app.py              # Главное приложение Flask
├── config.py           # Конфигурация подключения к БД
├── models.py          # Модели базы данных
├── create_db.py       # Скрипт создания таблиц
├── requirements.txt   # Зависимости Python
├── README.md          # Документация проекта
├── templates/         # HTML шаблоны
│   ├── base.html
│   ├── schedule.html
│   ├── add_schedule.html
│   ├── teachers.html
│   ├── add_teacher.html
│   ├── rooms.html
│   ├── add_room.html
│   ├── disciplines.html
│   ├── add_discipline.html
│   ├── groups.html
│   └── add_group.html
└── tests/            # Тесты pytest
    ├── __init__.py
    └── test_app.py
```

## Использование

1. Перейдите по адресу http://127.0.0.1:5000
2. Добавьте данные в справочники:
   - Преподаватели (ФИО, должность)
   - Аудитории (номер, корпус, вместимость)
   - Дисциплины (название)
   - Группы (название, специальность)
3. Создайте расписание занятий
4. При добавлении занятий система автоматически проверяет пересечения
Год: 2026
